services:
  cloud-mail:
    build: .
    container_name: cloud-mail-vps
    restart: unless-stopped
    environment:
      NO_D1_WARNING: "true"
    ports:
      - "8000:8000"
    volumes:
      - cloud_mail_data:/data

volumes:
  cloud_mail_data:
